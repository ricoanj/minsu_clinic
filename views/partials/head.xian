<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="MBC Bongabong Campus Clinic — student health services, consultations, and medicine dispensing." />
  <link rel="icon" href="/favicon.ico" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />
  <title>MBC Bongabong Campus Clinic - {{title}}</title>
  <style>
    :root {
      --brand: #0ea5a4;
      --brand-600: #059669;
      --brand-700: #047857;
      --topbar-height: 4rem; /* reserve space for the fixed top navbar */
    }

    html, body {
      height: 100%;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    body {
      background-color: #f8fafc;
    }

    .sidebar-link {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.75rem;
      border-radius: 0.375rem;
      color: inherit;
      text-decoration: none;
    }

    .sidebar-link:hover {
      background-color: #ecfeff;
    }

    .sidebar-link.active,
    .sidebar-link[aria-current="true"] {
      background-color: #ccfbf1;
      color: #0f766e;
      font-weight: 600;
    }

    /* Fixed top navbar: reserve space so page content is not hidden behind it */
    body {
      padding-top: var(--topbar-height);
    }

    .main-shift {
      transition: padding-top 200ms ease;
      padding-top: var(--topbar-height);
    }

    body.dark {
      background-color: #0b1220;
      color: #e6eef6;
    }

    body.dark .bg-white {
      background-color: #0f1724 !important;
    }

    body.dark .text-gray-600,
    body.dark .text-gray-500 {
      color: #94a3b8 !important;
    }

    body.dark .border {
      border-color: rgba(255, 255, 255, 0.06) !important;
    }

    body.dark .shadow-md {
      box-shadow: 0 1px 3px rgba(2, 6, 23, 0.6) !important;
    }

    body.dark .sidebar-link:hover {
      background-color: rgba(16, 24, 39, 0.6);
    }

    body.dark .sidebar-link {
      color: #cbd5e1;
    }
  </style>
</head>